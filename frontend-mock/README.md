# Archived integration prototype

Use [the current frontend](../frontend/README.md) and root Docker Compose.
This directory is historical source only. Its local SDK dependencies and Dockerfile
require the old reference IFC Lite checkout, which has been removed.
The current app uses locked published IFC Lite packages and requires no submodule.
